# sphinx-doc-build(for supersetx.com)
sphinx-doc-build allows you to build *Sphinx documentation* using a Docker.

```sh
docker build -it supersetx/sphinx-doc-build .
cd docs/docs
sh build.sh
exit
```